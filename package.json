{
  "name": "chain-chapters",
  "private": true,
  "version": "0.3.6",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build && node scripts/build-extension.mjs",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@types/chrome": "latest",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0",
    "vite": "^8.2.2",
    "esbuild": "latest",
    "vitest": "^4.1.11"
  }
}
